# Archived material — see intermediate/README.md
intermediate/

# Python
__pycache__/
*.egg-info/
.venv/

*.joblib
*.pkl
*.pickle

# Trained probe/decoder weights are regenerated by scripts, not committed.
phase_2/artifacts/**/*.pt

# NOTE: models/checkpoints/ is deliberately NOT ignored. The TabSwift weights
# there are the only copy we have (upstream HF repo is gated), so they are
# tracked. See models/README.md.

# R_real: cached OpenML download, regenerated on first use by
# experiments/q1/tasks.py, not committed.
experiments/q1/_data_cache/

# ---------------------------------------------------------------------------
# Ad-hoc session junk at the repo root. These are debug one-offs and syscall
# dumps from the August audit runs, not results. The measurement records live
# under experiments/*/results*.json and experiments/*/*.npz, which ARE tracked.
# ---------------------------------------------------------------------------
/strace.out
/batch1.log
/batch1_new.log
/batch1_seq.log
/batch2.log
/batch2_new.log
/batch3.log
/batch3_new.log
/batch4_err.txt
/batch4_out.txt
/test_tabicl.py
/test_tabicl.out
/test_tabicl.err
/test_profile.py
/run.sh
